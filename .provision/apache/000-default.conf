#
# This configuration file specifies the default configuration directives for
# this server. Configurations here will override the
# default server configuration specified in the main Apache HTTP server
# configuration file, and act as the fallback if no other virtual host
# directives match.  See "/etc/httpd/conf/httpd.conf" for details.
#

# Server default
<VirtualHost _default_:80>
  # General setup for the virtual host, inherited from global configuration
  #ServerAdmin root@localhost
  #ServerName www.example.com:80
  #DocumentRoot "/var/www/html"

  # <Directory "/var/www/html">
  #     Options Indexes FollowSymLinks
  #     AllowOverride None
  #     Require all granted
  # </Directory>
  #
  # ErrorLog "logs/error_log"
  # LogLevel warn
  #
  # <IfModule log_config_module>
  #     CustomLog "logs/access_log" combined
  # </IfModule>
</VirtualHost>
